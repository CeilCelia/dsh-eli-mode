# 初始记忆

本条目是 `mem/` 目录的示例——mem 放"跨会话要记住的东西"。

## 可以放什么

- 用户偏好（沟通风格、常用工具、习惯）
- 进行中的项目与其状态
- 关键决策和约定

## 怎么新建记忆

`kb_write` 标题 + 内容，id 用 `mem/xxx`（小写路径，无 `.md` 后缀）。写完后目录树会自动出现 `mem/` 下的新条目。

规范详见 [[memory-guide]]。
